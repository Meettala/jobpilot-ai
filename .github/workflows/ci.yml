name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - name: Regenerate dependency lockfile
        run: npm install --package-lock-only --ignore-scripts

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Build production application
        run: npm run build

      - name: Capture production audit
        run: |
          set +e
          npm audit --omit=dev --audit-level=high --json > npm-audit.json
          status=$?
          echo "$status" > npm-audit-status.txt
          exit 0

      - name: Upload generated lockfile and audit
        uses: actions/upload-artifact@v4
        with:
          name: dependency-verification
          path: |
            package-lock.json
            npm-audit.json
            npm-audit-status.txt
          retention-days: 3

      - name: Enforce production audit
        run: test "$(cat npm-audit-status.txt)" -eq 0

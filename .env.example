# Optional. Without either, cover letters are drafted template-only
# (still fully evidence-grounded — just no LLM prose generation).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Needed once the app is wired to persist jobs/evidence to Supabase
# (not required for the current in-memory demo mode).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
